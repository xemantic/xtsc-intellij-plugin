<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- The id must not contain a JetBrains product name as a dot-separated component
         ("intellij" among them), so it cannot mirror the group and package. -->
    <id>com.xemantic.xtsc</id>
    <name>xtsc</name>
    <vendor url="https://xemantic.com" email="morisil@xemantic.com">Xemantic</vendor>
    <description><![CDATA[
        <p>TypeScript code insight backed by <b>xtsc</b> &mdash; the TypeScript scanner, parser, binder,
        type checker, transformer and emitter rewritten from scratch in Kotlin.</p>

        <p>The compiler runs inside the IDE process. There is no Node.js runtime, no
        <code>tsserver</code> child process, and no language server protocol between the editor and the
        type checker &mdash; the IDE asks the compiler for hover, completion, navigation and diagnostics
        through a direct function call, against unsaved editor buffers.</p>

        <p>Planned capabilities:</p>
        <ul>
            <li>Whole-program type checking driven by <code>tsconfig.json</code></li>
            <li>Hover, go to definition, find usages, completion and signature help</li>
            <li>Rename that is re-checked before it is applied</li>
            <li>Diagnostics matching those reported by <code>tsc</code></li>
        </ul>

        <p>This plugin is in early development and does not implement the above yet. Follow its progress
        in the <a href="https://github.com/xemantic/xtsc-intellij-plugin">plugin repository</a>, and read
        about the compiler behind it in the
        <a href="https://github.com/xemantic/xemantic-typescript-compiler">xtsc repository</a>.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <resource-bundle>messages.MyBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow factoryClass="com.xemantic.xtsc.intellij.toolWindow.MyToolWindowFactory" id="MyToolWindow"/>
        <postStartupActivity implementation="com.xemantic.xtsc.intellij.startup.MyProjectActivity" />
    </extensions>
</idea-plugin>
